g.V().hasLabel ( 'BioProc:Concept:Resource' ).as ( 'class' )
  .order ().by ( shuffle )
  .repeat ( out ( 'is_a' ).hasLabel ( 'BioProc:Concept:Resource' ) )
  .until ( outE ( 'is_a' ).count ().is ( 0 ) ).as ( 'top' )
.select ( 'class', 'top' )
.by ( id () )
// DEBUG .by ( 'prefName' )
.dedup ()
.limit ( 100 )
