g.V().hasLabel ( 'BioProc:Concept:Resource' ).as( 'class' )
  .order().by ( shuffle )
  .repeat( out ( 'is_a' ).hasLabel ( 'BioProc:Concept:Resource' ) ).times ( $steps )
  .as( 'top' )
.select ( 'class', 'top' )
.by ( id () )
// DEBUG .by ( 'prefName' )
.dedup ()
.limit ( 100 )
